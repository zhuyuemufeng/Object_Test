<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itheima.dao.system.ModuleMapper">

    <resultMap id="map1" type="module">
        <id column="module_id" property="id" />
        <result column="parent_id" property="parentId" />
        <result column="parent_name" property="parentName" />
        <result column="name" property="name" />
        <result column="layer_num" property="layerNum" />
        <result column="is_leaf" property="isLeaf" />
        <result column="ico" property="ico" />
        <result column="cpermission" property="cpermission" />
        <result column="curl" property="curl" />
        <result column="ctype" property="ctype" />
        <result column="state" property="state" />
        <result column="belong" property="belong" />
        <result column="cwhich" property="cwhich" />
        <result column="quote_num" property="quoteNum" />
        <result column="remark" property="remark" />
        <result column="order_no"  property="orderNo" />
    </resultMap>

    <select id="findAll" parameterType="string" resultMap="map1">
        SELECT * FROM ss_module
    </select>

    <select id="findById" resultMap="map1" parameterType="string">
        SELECT * FROM ss_module where module_id = #{id}
    </select>

    <update id="update" parameterType="module">
        update ss_module
        set
        parent_name = #{parentName},
        name = #{name},
        layer_num = #{layerNum},
        is_leaf = #{isLeaf},
        ico = #{ico},
        cpermission = #{cpermission},
        curl = #{curl},
        ctype = #{ctype},
        state = #{state},
        belong = #{belong},
        cwhich = #{cwhich},
        quote_num = #{quoteNum},
        remark = #{remark},
        order_no = #{orderNo}
        where module_id = #{id}
    </update>

    <insert id="save" parameterType="module">
        insert into ss_module (module_id, parent_id, parent_name, name, layer_num, is_leaf,
        ico, cpermission, curl, ctype, state, belong, cwhich, quote_num,
        remark, order_no)
        values (#{id}, #{parentId}, #{parentName}, #{name}, #{layerNum}, #{isLeaf},
        #{ico}, #{cpermission}, #{curl}, #{ctype}, #{state}, #{belong}, #{cwhich}, #{quoteNum},
        #{remark}, #{orderNo})
    </insert>

    <delete id="delete" parameterType="string">
        DELETE FROM ss_module where module_id = #{id}
    </delete>

    <select id="findUserModule" resultMap="map1" parameterType="string">
        SELECT * FROM ss_module WHERE module_id IN (
        SELECT pm.module_id FROM pe_role_user pu,pe_role_module pm
        WHERE pu.role_id = pm.role_id AND pu.user_id = #{userId}
        )
    </select>

    <select id="findSystemModule" resultMap="map1" parameterType="string">
        SELECT * from ss_module where belong = #{num}
    </select>
    
    <select id="findAdminModule" resultMap="map1">
        SELECT * from ss_module
    </select>
</mapper>